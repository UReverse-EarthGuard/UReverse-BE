<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.earth.ureverse.global.mapper.ProductMapper">

    <!-- FINISH 상태 상품 목록 조회 -->
    <select id="getFinishProducts" resultType="com.earth.ureverse.admin.dto.response.FinishProductResponse">
        SELECT
            p.product_id AS id,
            (
            SELECT pi.url
            FROM product_image pi
            WHERE pi.product_id = p.product_id
            AND ROWNUM = 1
            ) AS image,
            b.name AS brand,
            c.name AS category,
            p.grade AS grade,
            TO_CHAR(p.updated_at, 'YYYY-MM-DD HH24:MI:SS') AS finishDate
        FROM product p
            JOIN brand b ON p.brand_id = b.brand_id
            JOIN category c ON p.category_id = c.category_id
        WHERE p.status = 'FINISH'
    </select>

    <!-- 검수 완료 상품 검색 -->
    <select id="getInspectionCompletedProductsByInspectorAndKeyword"
            resultType="com.earth.ureverse.inspector.dto.response.ProductSearchResultDto">

        SELECT
        p.product_id AS productId,
        b.name AS brandName,
        c.name AS categoryName,
        p.status AS status,
        img.url AS imageUrl,
        TO_CHAR(p.created_at, 'YYYY-MM-DD HH24:MI:SS') AS createdAt,
        bcc.point AS expectedPoint
        FROM product p
        JOIN brand b ON p.brand_id = b.brand_id
        JOIN category c ON p.category_id = c.category_id
        JOIN brand_category_combination bcc
        ON p.brand_id = bcc.brand_id
        AND p.category_id = bcc.category_id
        AND bcc.inspector_id = #{inspectorId}
        LEFT JOIN (
        SELECT product_id,
        MIN(url) KEEP (DENSE_RANK FIRST ORDER BY created_at) AS url
        FROM product_image
        GROUP BY product_id
        ) img ON img.product_id = p.product_id
        WHERE p.status IN ('REJECT', 'SECOND_INSPECT', 'DELIVERY_REQUEST', 'DELIVERING', 'FINISH')
        <if test="keyword != null and keyword != ''">
            AND (LOWER(c.name) LIKE '%' || LOWER(#{keyword}) || '%'
            OR LOWER(b.name) LIKE '%' || LOWER(#{keyword}) || '%')
        </if>
        ORDER BY p.created_at DESC
        OFFSET #{offset} ROWS FETCH NEXT #{pageSize} ROWS ONLY

    </select>

    <!-- 검수 미완료 상품 검색 -->
    <select id="getPendingInspectionProductsByInspectorAndKeyword"
            resultType="com.earth.ureverse.inspector.dto.response.ProductSearchResultDto">

        SELECT
        p.product_id AS productId,
        b.name AS brandName,
        c.name AS categoryName,
        p.status AS status,
        img.url AS imageUrl,
        TO_CHAR(p.created_at, 'YYYY-MM-DD HH24:MI:SS') AS createdAt,
        bcc.point AS expectedPoint
        FROM product p
        JOIN brand b ON p.brand_id = b.brand_id
        JOIN category c ON p.category_id = c.category_id
        JOIN brand_category_combination bcc
        ON p.brand_id = bcc.brand_id
        AND p.category_id = bcc.category_id
        AND bcc.inspector_id = #{inspectorId}
        LEFT JOIN (
        SELECT product_id,
        MIN(url) KEEP (DENSE_RANK FIRST ORDER BY created_at) AS url
        FROM product_image
        GROUP BY product_id
        ) img ON img.product_id = p.product_id
        WHERE p.status = 'FIRST_INSPECT'
        <if test="keyword != null and keyword != ''">
            AND (LOWER(c.name) LIKE '%' || LOWER(#{keyword}) || '%'
            OR LOWER(b.name) LIKE '%' || LOWER(#{keyword}) || '%')
        </if>
        ORDER BY p.created_at DESC
        OFFSET #{offset} ROWS FETCH NEXT #{pageSize} ROWS ONLY

    </select>

    <!-- 상태 CANCEL 아닌 상품 목록 조회 (paid_point, updated_at) -->
    <select id="getPickupProducts" resultType="com.earth.ureverse.admin.dto.response.PickupProductResponse">
        SELECT
        p.product_id AS id,
        (
        SELECT pi.url
        FROM product_image pi
        WHERE pi.product_id = p.product_id
        AND ROWNUM = 1
        ) AS image,
        b.name AS brand,
        c.name AS category,
        p.grade AS grade,
        p.paid_point AS paid_point,
        p.status AS status,
        TO_CHAR(p.updated_at, 'YYYY-MM-DD HH24:MI:SS') AS updatedAt
        FROM product p
        JOIN brand b ON p.brand_id = b.brand_id
        JOIN category c ON p.category_id = c.category_id
        WHERE p.status = 'REGISTER'
            OR p.status = 'FIRST_INSPECT'
            OR p.status = 'SECOND_INSPECT'
            OR p.status = 'DELIVERY_REQUEST'
            OR p.status = 'DELIVERING'
            OR p.status = 'FINISH'
        ORDER BY p.updated_at DESC
    </select>

    <!-- 상품 이미지 목록   -->
    <select id="getProductImages"
            parameterType="long"
            resultType="string">
        SELECT url
        FROM product_image
        WHERE product_id = #{productId}
        ORDER BY image_id
    </select>

    <!-- 수거 완료된 상품 상세 정보   -->
    <select id="getFinishProductDetail"
            parameterType="long"
            resultType="com.earth.ureverse.admin.dto.response.ProductInspectionResultResponse">
        SELECT
        p.product_id              AS "product.id",
        b.name                    AS "product.brand",
        c.main_name               AS "product.categoryMain",
        c.sub_name                AS "product.categorySub",
        p.paid_point              AS "product.paidPoint",
        bcc.point                 AS "product.expectPoint",
        u.email                   AS "product.userEmail",
        p.status                  AS "product.status",
        TO_CHAR(p.updated_at, 'YYYY-MM-DD') AS "product.updatedAt",

        -- AI 검사 결과
        inspectorUser.email       AS "aiResult.inspectorEmail",
        DECODE(aiins.is_torn, 'Y', 1, 0)        AS "aiResult.isTorn",
        DECODE(aiins.has_stain, 'Y', 1, 0)       AS "aiResult.hasStain",
        DECODE(aiins.has_fading, 'Y', 1, 0)      AS "aiResult.hasFading",
        DECODE(aiins.is_stretched, 'Y', 1, 0)  AS "aiResult.isStretched",
        aiins.notes               AS "aiResult.note",

        -- Human 검사 결과
        inspectorUser.email     AS "inspectorResult.inspectorEmail",
        DECODE(huins.is_torn, 'Y', 1, 0)        AS "inspectorResult.isTorn",
        DECODE(huins.has_stain, 'Y', 1, 0)       AS "inspectorResult.hasStain",
        DECODE(huins.has_fading, 'Y', 1, 0)      AS "inspectorResult.hasFading",
        DECODE(huins.is_stretched, 'Y', 1, 0)  AS "inspectorResult.isStretched",
        huins.notes               AS "inspectorResult.note",

        -- 최종 grade
        p.grade                   AS grade

        FROM product p
        JOIN users u ON p.user_id = u.user_id
        JOIN brand b ON p.brand_id = b.brand_id
        JOIN category c ON p.category_id = c.category_id
        LEFT JOIN brand_category_combination bcc
        ON p.brand_id = bcc.brand_id AND p.category_id = bcc.category_id
        LEFT JOIN inspection aiins ON aiins.product_id = p.product_id AND aiins.inspect_method = 'AI'
        LEFT JOIN inspection huins ON huins.product_id = p.product_id AND huins.inspect_method = 'Human'
        LEFT JOIN users inspectorUser ON bcc.inspector_id = inspectorUser.user_id

        WHERE p.status = 'FINISH'
        AND p.product_id = #{productId}
    </select>


</mapper>